{% extends 'admin/base.html.twig' %}

{% block title %}{{ isNew ? 'Nouvelle catégorie' : 'Modifier ' ~ category.name }}{% endblock %}
{% block page_title %}{{ isNew ? 'Nouvelle catégorie' : 'Modifier la catégorie' }}{% endblock %}

{% form_theme form 'bootstrap_5_layout.html.twig' %}

{% block body %}

<div class="bg-white rounded-viefit border p-4" style="max-width: 700px;">
    {{ form_start(form) }}
        {{ form_row(form.name) }}
        {{ form_row(form.description) }}

        {% if not isNew and category.image %}
            <div class="mb-2">
                <img src="{{ asset('uploads/categories/' ~ category.image) }}" alt="" style="width:100px;height:100px;object-fit:cover;border-radius:0.35rem;">
            </div>
        {% endif %}
        {{ form_row(form.imageFile) }}
        {{ form_row(form.isActive) }}

        <div class="mt-4 d-flex gap-2">
            <button type="submit" class="btn btn-teal px-4">Enregistrer</button>
            <a href="{{ path('admin_category_index') }}" class="btn btn-outline-secondary">Annuler</a>
        </div>
    {{ form_end(form) }}
</div>

{% endblock %}
